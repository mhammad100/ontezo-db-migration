export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
  DOCUMENT = 'document',
  OTHER = 'other',
}

export enum Tag {
  COMMENT = 'comment',
  USER = 'user',
  PROJECT = 'project',
  TASK = 'task',
}
